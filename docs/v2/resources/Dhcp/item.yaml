get:
  parameters:
    - $ref: '#/components/parameters/DhcpId'
  responses:
    200:
      description: success
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: '#/components/schemas/Dhcp'
              meta:
                type: object
  tags:
    - Dhcps
patch:
  parameters:
    - $ref: '#/components/parameters/DhcpId'
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          allOf:
            - $ref: "#/components/schemas/Dhcp"
            - type: object
              properties:
                vpc_id:
                  required: false
  responses:
    200:
      $ref: '#/components/responses/DhcpIdResponse'
  tags:
    - Dhcps
delete:
  parameters:
    - $ref: '#/components/parameters/DhcpId'
  responses:
    204:
      description: Success
  tags:
    - Dhcps
