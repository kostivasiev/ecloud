get:
  parameters:
    - $ref: 'parameters/DhcpId.yaml'
  responses:
    200:
      description: success
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: 'schemas/Dhcp.yaml'
              meta:
                type: object
  tags:
    - Dhcps
patch:
  parameters:
    - $ref: 'parameters/DhcpId.yaml'
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          allOf:
            - $ref: "../../public-openapi.yaml#/components/schemas/Dhcp"
            - type: object
              properties:
                vpc_id:
                  required: false
                availability_zone_id:
                  required: false
  responses:
    200:
      $ref: 'responses/DhcpIdResponse.yaml'
  tags:
    - Dhcps
delete:
  parameters:
    - $ref: 'parameters/DhcpId.yaml'
  responses:
    204:
      description: Success
  tags:
    - Dhcps
