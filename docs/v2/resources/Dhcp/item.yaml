get:
  parameters:
    - $ref: 'parameters/DhcpId'
  responses:
    200:
      description: success
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: 'schemas/Dhcp'
              meta:
                type: object
  tags:
    - Dhcps
patch:
  parameters:
    - $ref: 'parameters/DhcpId'
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          allOf:
            - $ref: "schemas/Dhcp"
            - type: object
              properties:
                vpc_id:
                  required: false
                availability_zone_id:
                  required: false
  responses:
    200:
      $ref: 'responses/DhcpIdResponse'
  tags:
    - Dhcps
delete:
  parameters:
    - $ref: 'parameters/DhcpId'
  responses:
    204:
      description: Success
  tags:
    - Dhcps
