post:
  parameters :
    - $ref : '../../resources/Volume/parameters/volumeId.yaml'
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/volume/detach.yaml'
  responses:
    202:
      description: Accepted
    422:
      description: "Precondition Failed"
  tags:
    - Volumes
