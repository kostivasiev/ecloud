get:
  parameters:
    - $ref: 'parameters/VolumeId.yaml'
  responses:
    200:
      description: success
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: 'schemas/Volume.yaml'
              meta:
                type: object
  tags:
    - Volumes
patch:
  parameters:
    - $ref: 'parameters/VolumeId.yaml'
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          allOf:
            - $ref: "../../public-openapi.yaml#/components/schemas/Volume"
            - type: object
              properties:
                vpc_id:
                  required: false
                capacity:
                  required: false
  responses:
    200:
      $ref: 'responses/VolumeIdResponse.yaml'
  tags:
    - Volumes
delete:
  parameters:
    - $ref: 'parameters/VolumeId.yaml'
  responses:
    204:
      description: Success
  tags:
    - Volumes
