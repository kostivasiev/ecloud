get:
  parameters:
    - $ref: 'parameters/VolumeId'
  responses:
    200:
      description: success
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: 'schemas/Volume'
              meta:
                type: object
  tags:
    - Volumes
patch:
  parameters:
    - $ref: 'parameters/VolumeId'
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          allOf:
            - $ref: "schemas/Volume"
            - type: object
              properties:
                vpc_id:
                  required: false
                capacity:
                  required: false
  responses:
    200:
      $ref: 'responses/VolumeIdResponse'
  tags:
    - Volumes
delete:
  parameters:
    - $ref: 'parameters/VolumeId'
  responses:
    204:
      description: Success
  tags:
    - Volumes
