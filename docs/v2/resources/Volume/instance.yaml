get:
  parameters:
    - $ref: '../../resources/Volume/parameters/volumeId.yaml'
  responses:
    200:
      description: success
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: '../../components/schemas/instance.yaml'
              meta:
                type: object
  tags:
    - Volumes
