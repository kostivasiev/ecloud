post:
  parameters :
    - $ref : '../../resources/Volume/parameters/volumeId.yaml'
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/volume/attach.yaml'
  responses:
    202:
      description: Accepted
  tags:
    - Volumes
