get:
  parameters:
    - $ref: 'parameters/AvailabilityZoneId.yaml'
  responses:
    200:
      description: success
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: 'schemas/AvailabilityZone.yaml'
              meta:
                type: object
  tags:
    - AvailabilityZones
