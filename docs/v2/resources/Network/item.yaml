get:
  parameters:
    - $ref: '#/components/parameters/NetworkId'
  responses:
    200:
      description: success
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: '#/components/schemas/Network'
              meta:
                type: object
  tags:
    - Networks

patch:
  parameters:
    - $ref: '#/components/parameters/NetworkId'
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          allOf:
            - $ref: "#/components/schemas/Network"
            - type: object
              properties:
                name:
                  required: false
                router_id:
                  required: false
                availability_zone_id:
                  required: false
  responses:
    200:
      $ref: '#/components/responses/NetworksIdResponse'
  tags:
    - Networks

delete:
  parameters:
    - $ref: '#/components/parameters/NetworkId'
  responses:
    204:
      description: Success
  tags:
    - Networks
