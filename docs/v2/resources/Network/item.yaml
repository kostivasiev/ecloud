get:
  parameters:
    - $ref: 'parameters/NetworkId'
  responses:
    200:
      description: success
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: 'schemas/Network'
              meta:
                type: object
  tags:
    - Networks

patch:
  parameters:
    - $ref: 'parameters/NetworkId'
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          allOf:
            - $ref: "schemas/Network"
            - type: object
              properties:
                name:
                  required: false
                router_id:
                  required: false
  responses:
    200:
      $ref: 'responses/NetworksIdResponse'
  tags:
    - Networks

delete:
  parameters:
    - $ref: 'parameters/NetworkId'
  responses:
    204:
      description: Success
  tags:
    - Networks
