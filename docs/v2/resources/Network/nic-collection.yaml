get:
  parameters:
    - $ref: '../../resources/Network/parameters/networkId.yaml'
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: '../../admin-openapi.yaml#/components/schemas/Nics'
              meta:
                type: object
  tags:
    - Networks
