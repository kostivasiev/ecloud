get:
  parameters:
    - $ref: '../../resources/HostSpec/parameters/hostSpecId.yaml'
  responses:
    200:
      description: success
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: '../../resources/HostSpec/schema.yaml'
              meta:
                type: object
  tags:
    - Host Spec
