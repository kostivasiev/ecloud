get:
  parameters:
    - $ref: '../../components/parameters/perPage.yaml'
    - $ref: '../../components/parameters/page.yaml'
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: '../../public-openapi.yaml#/components/schemas/NetworkRule'
              meta:
                allOf:
                  - $ref: "../../public-openapi.yaml#/components/schemas/Meta"
                  - type: object
                    properties:
                      pagination:
                        properties:
                          links:
                            type: object
                            properties:
                              first:
                                example: 'https://api.ukfast.io/ecloud/v2/network-rules?page=1'
                              previous:
                                example: 'https://api.ukfast.io/ecloud/v2/network-rules?page=1'
                              next:
                                example: 'https://api.ukfast.io/ecloud/v2/network-rules?page=2'
                              last:
                                example: 'https://api.ukfast.io/ecloud/v2/network-rules?page=2'
  tags:
    - Network Rule

post:
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/networkRule.yaml'
  responses:
    202:
      description: Accepted
      $ref: '../../resources/NetworkRule/responses/idResponse.yaml'
  tags:
    - Network Rule
