get:
  parameters:
    - $ref: 'parameters/LbcId.yaml'
  responses:
    200:
      description: success
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: 'schemas/LoadBalancerCluster.yaml'
              meta:
                type: object
  tags:
    - Load Balancer Clusters
patch:
  parameters:
    - $ref: 'parameters/LbcId.yaml'
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          allOf:
            - $ref: "../../public-openapi.yaml#/components/schemas/LoadBalancerCluster"
            - type: object
              properties:
                vpc_id:
                  required: false
                nodes:
                  required: false
  responses:
    200:
      $ref: 'responses/LbcIdResponse.yaml'
  tags:
    - Load Balancer Clusters
delete:
  parameters:
    - $ref: 'parameters/LbcId.yaml'
  responses:
    204:
      description: Success
  tags:
    - Load Balancer Clusters
