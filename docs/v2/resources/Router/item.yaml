get:
  parameters:
    - $ref: '#/components/parameters/RouterId'
  responses:
    200:
      description: success
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: '#/components/schemas/Router'
              meta:
                type: object
  tags:
    - Routers
patch:
  parameters:
    - $ref: '#/components/parameters/RouterId'
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          allOf:
            - $ref: "#/components/schemas/Router"
            - type: object
              properties:
                name:
                  required: false
                vpc_id:
                  required: false
  responses:
    200:
      $ref: '#/components/responses/RouterIdResponse'
  tags:
    - Routers
delete:
  parameters:
    - $ref: '#/components/parameters/RouterId'
  responses:
    204:
      description: Success
  tags:
    - Routers
