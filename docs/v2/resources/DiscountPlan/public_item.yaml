get:
  parameters:
    - $ref: '../../resources/DiscountPlan/parameters/discountPlanId.yaml'
  responses:
    200:
      description: success
      content:
        application/json:
          schema:
            type: object
            allOf:
              - $ref: '../../components/schemas/discountPlan.yaml'
  tags:
    - Discount Plans