get:
  parameters:
    - $ref: '../../resources/ResourceTier/parameters/id.yaml'
  responses:
    200:
      description: success
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: '../../resources/ResourceTier/schema.yaml'
              meta:
                type: object
  tags:
    - Resource Tiers
