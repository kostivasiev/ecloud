get:
  parameters:
    - $ref: 'parameters/RegionId.yaml'
  responses:
    200:
      description: success
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: 'schemas/Region.yaml'
              meta:
                type: object
  tags:
    - Regions
