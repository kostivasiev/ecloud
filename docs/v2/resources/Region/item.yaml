get:
  parameters:
    - $ref: 'parameters/RegionId'
  responses:
    200:
      description: success
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: 'schemas/Region'
              meta:
                type: object
  tags:
    - Regions
