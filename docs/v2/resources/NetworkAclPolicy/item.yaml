get:
  parameters:
    - $ref: '../../public-openapi.yaml#/components/parameters/NetworkAclPolicyId'
  responses:
    200:
      description: success
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: '../../public-openapi.yaml#/components/schemas/NetworkAclPolicy'
              meta:
                type: object
  tags:
    - ACL Policy

patch:
  parameters:
    - $ref: '../../resources/NetworkAclPolicy/parameters/networkAclPolicyId.yaml'
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          allOf:
            - $ref: '../../components/schemas/networkAclPolicy.yaml'
            - type: object
              properties:
                name:
                  required: false
                router_id:
                  required: false
  responses:
    200:
      $ref: '../../resources/NetworkAclPolicy/responses/idResponse.yaml'
  tags:
    - ACL Policy

delete:
  parameters:
    - $ref: '../../resources/NetworkAclPolicy/parameters/networkAclPolicyId.yaml'
  responses:
    204:
      description: Success
  tags:
    - ACL Policy
