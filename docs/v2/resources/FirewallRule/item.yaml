get:
  parameters:
    - $ref: '#/components/parameters/FirewallRuleId'
  responses:
    200:
      description: success
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: '#/components/schemas/FirewallRule'
              meta:
                type: object
  tags:
    - Firewall Rules
patch:
  parameters:
    - $ref: '#/components/parameters/FirewallRuleId'
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          allOf:
            - $ref: "#/components/schemas/FirewallRule"
            - type: object
              properties:
                name:
                  required: false
                router_id:
                  required: false
  responses:
    200:
      $ref: '#/components/responses/FirewallRuleIdResponse'
  tags:
    - Firewall Rules
delete:
  parameters:
    - $ref: '#/components/parameters/FirewallRuleId'
  responses:
    204:
      description: Success
  tags:
    - Firewall Rules

