get:
  parameters:
    - $ref: 'parameters/FipId'
  responses:
    200:
      description: success
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: 'schemas/FloatingIP'
              meta:
                type: object
  tags:
    - Floating IPs
patch:
  parameters:
    - $ref: 'parameters/FipId'
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          allOf:
            - $ref: "schemas/FloatingIP"
  responses:
    200:
      $ref: 'responses/FipIdResponse'
  tags:
    - Floating IPs
delete:
  parameters:
    - $ref: 'parameters/FipId'
  responses:
    204:
      description: Success
  tags:
    - Floating IPs

