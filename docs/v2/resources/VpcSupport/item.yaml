get:
  parameters:
    - $ref: '../../resources/VpcSupport/parameters/vpcSupportId.yaml'
  responses:
    200:
      description: success
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: '../../components/schemas/vpcSupport.yaml'
              meta:
                type: object
  tags:
    - Support

patch:
  parameters:
    - $ref: '../../resources/VpcSupport/parameters/vpcSupportId.yaml'
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          allOf:
            - $ref: '../../components/schemas/vpcSupport.yaml'
    200:
      $ref: '../../resources/VpcSupport/responses/idResponse.yaml'
  tags:
    - Support

delete:
  description: Disable support for a VPC
  parameters:
    - $ref: '../../resources/VpcSupport/parameters/vpcSupportId.yaml'
  responses:
    204:
      description: Success
  tags:
    - Support
