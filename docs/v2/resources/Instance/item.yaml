get:
  parameters:
    - $ref: 'parameters/InstanceId'
  responses:
    200:
      description: success
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: 'schemas/Instance'
              meta:
                type: object
  tags:
    - Instances
patch:
  parameters:
    - $ref: 'parameters/InstanceId'
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          allOf:
            - $ref: "schemas/Instance"
  responses:
    200:
      $ref: 'responses/InstanceIdResponse'
  tags:
    - Instances
delete:
  parameters:
    - $ref: 'parameters/InstanceId'
  responses:
    204:
      description: Success
  tags:
    - Instances
